<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.github.jason-te-sde</groupId>
    <artifactId>till-parent</artifactId>
    <version>0.1.0</version>
  </parent>

  <artifactId>till-testkit</artifactId>
  <name>till-testkit</name>
  <description>
    A deterministic concurrency simulator, the invariants it checks after every step, and a set of
    known flaws it is proven to catch. Usable against any Ledger, including your own.
  </description>

  <dependencies>
    <dependency>
      <groupId>io.github.jason-te-sde</groupId>
      <artifactId>till-core</artifactId>
    </dependency>
    <!-- Deliberately no test framework in the main scope. The simulator raises
         InvariantViolation, so a caller can drive it from JUnit, from a main method, or from a
         fuzzing harness without inheriting a dependency on the runner this project happens to use. -->
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-simple</artifactId>
      <version>${slf4j.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
